[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mmud"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
